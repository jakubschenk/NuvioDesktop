# 0.1.24 rebase deferred commits

Base tag: `0.1.24`
Working branch: `windows-layout-test`

This file records commits intentionally not replayed during the rebase because
they conflicted with newer `0.1.24` work, risked overwriting upstream behavior,
or duplicated functionality already present upstream.

## Deferred

- `d3e10b3a` - `Fix desktop image scaling quality`
  - Deferred during rebase because it conflicted with newer upstream image
    loading changes across shared UI, details, player, profile, and desktop
    home image components.
  - `0.1.24` already contains Coil-based image request sizing, TMDB quality
    upgrades, cloud-library artwork handling, and related filter-quality hooks
    in the same call sites. Replaying this commit would replace or duplicate
    those newer implementations.
  - Kept the `0.1.24` implementation for now, per the policy to keep upstream
    when both sides implemented the same behavior.

- `3175d20c` - `Tune home image sizing`
  - Deferred because it conflicted in `HomeHeroSection` and
    `HomeContinueWatchingSection`, where `0.1.24` already has newer home image
    sizing, artwork selection, and image quality handling.
  - Replaying it would overwrite or fork the upstream hero/continue-watching
    image pipeline, so the upstream implementation is kept for now.

- `b49b39cc` - `Expand desktop hero height`
  - Deferred because it conflicts directly with the `0.1.24` home hero layout
    constants and related tests.
  - The newer release already changed this area, so preserving upstream avoids
    overwriting current hero sizing behavior with an older desktop-only tweak.

- `66266898` - `Increase desktop hero height`
  - Deferred for the same reason as `b49b39cc`: it conflicts in
    `HomeHeroSection` and its tests, and would override the newer release hero
    sizing decisions.

- `dbdeca74` - `Improve desktop image rendering`
  - Deferred because it modifies the custom `AsyncImage` wrapper introduced by
    skipped commit `d3e10b3a` and conflicts with `0.1.24` desktop image
    rendering in `HomeHeroSection` and collection card rendering.
  - Keeping upstream avoids reintroducing an older parallel image-loading path.

- `8ea5ee9f` - `Improve desktop mouse and keyboard interactions`
  - Deferred because it conflicts across newer `0.1.24` details, home, player
    controls, player screen, and stream/source panel code.
  - The commit is a broad interaction pass over surfaces that changed
    substantially upstream. Replaying it now would risk replacing current
    release behavior with older desktop interaction assumptions.

- `a33bea06` - `Refine desktop player controls`
  - Deferred because it conflicts with `0.1.24` player controls, player engine,
    player screen, and MPV backend code.
  - The release tag has newer player-control and backend behavior in the same
    areas, so this older refactor is not replayed in this pass.

- `1b80af88` - `Reshuffle desktop player chrome`
  - Deferred because it conflicts in `PlayerControls`, where `0.1.24` has newer
    player chrome behavior.
  - The commit also adds wall-clock plumbing, but replaying it as-is would
    require replacing current upstream player-control structure.

- `7b813587` - `Tune player time and volume controls`
  - Deferred because it conflicts in `PlayerControls` and `PlayerScreen`.
  - `0.1.24` has newer time/volume/player-screen behavior in those files; this
    older tuning pass is not replayed over it.

- `7d3d5b1d` - `Polish player controls and gestures`
  - Deferred because it conflicts in `PlayerControls` and `PlayerScreen`.
  - This overlaps the same player chrome and gesture surface already changed in
    `0.1.24`, so upstream is kept.

- `d40b1f66` - `Tune player timestamps and speed control`
  - Deferred because it conflicts with the release player controls, player
    screen, and MPV backend.
  - The affected timestamp/speed-control path has newer upstream changes, so it
    is not replayed in this rebase pass.

- `50b84a49` - `Fix next episode stream continuation`
  - Deferred because it conflicts with `0.1.24` player screen, stream
    repository, stream autoplay selector, and selector tests.
  - The release tag has newer stream continuation/autoplay behavior, so this
    older fix is left out pending a dedicated comparison.
  - 2026-05-26 follow-up: safely included the provider-preference fallback for
    next-episode stream continuation and added selector tests. Kept the newer
    `0.1.24` debrid-aware stream timeout/selection flow instead of replaying
    the older full commit over it.

- `53a9e1a9` - `Fix player controls regressions`
  - Deferred because it conflicts in `PlayerControls` and `PlayerScreen`.
  - The upstream player implementation has moved on since this regression fix;
    keeping release behavior avoids overwriting newer control flow.

- `24d0c573` - `Improve desktop player responsiveness`
  - Deferred because it conflicts with `0.1.24` player screen/backend code,
    desktop collection image rendering, and the `mediamp` submodule pointer.
  - The submodule histories do not merge trivially, and forcing this commit
    would risk replacing newer release MPV/player integration.

- `33e7bed4` - `Default player to fit and prefetch episode metadata`
  - Deferred because it conflicts with newer `0.1.24` app routing/player state
    wiring in `App.kt` and `PlayerScreen`.
  - The release tag owns the current player metadata and screen lifecycle flow;
    this older prefetch/default-fit patch is left out for now.

- `512a63c3` - `Optimize desktop screen list rendering`
  - Deferred because it conflicts across `HomeScreen`, continue-watching,
    library, and search screens, and it modifies the skipped custom
    `AsyncImage` wrapper.
  - `0.1.24` already changed these list/rendering surfaces; replaying this
    optimization would risk overwriting newer screen implementations.
  - 2026-05-26 follow-up: safely included the reusable `contentType` plumbing
    and compatible lazy-list/grid content type hints across home, catalog,
    collection, details, library, search, and stream screens. Also adapted the
    details-series resource-label cleanup to avoid blocking `getString` calls in
    composables. Kept the conflicting `AsyncImage` measurement/decode behavior
    and unrelated logo sizing changes deferred with the image/layout stack.

- `96f4235f` - `Decouple player volume scroll from input rate`
  - Deferred because it conflicts with `0.1.24` player episode panel, player
    screen, and source panel code.
  - The release tag has newer player input handling in these files, so this
    older volume-scroll decoupling pass is left out.
  - 2026-05-26 follow-up: safely included the coalesced player volume-scroll
    handling and the player list `contentType` hints by adapting them onto the
    current player files. The direct cherry-pick remains deferred because it
    would still overwrite newer release player wiring.

- `e495b9a0` - `Improve home scroll and renderer diagnostics`
  - Deferred because it conflicts with current `DesktopApp` renderer setup and
    the `mediamp` submodule pointer.
  - The submodule conflict is non-trivial, and the release tag has newer
    renderer/player bootstrap work that should not be overwritten in this pass.

- `63321db8` - `Revert "Improve home scroll and renderer diagnostics"`
  - Deferred because it is the revert of deferred commit `e495b9a0` and still
    conflicts with `DesktopApp` plus the `mediamp` submodule pointer.
  - Since the original commit was not replayed, replaying its revert is
    unnecessary and risks disturbing upstream renderer setup.

- `f2c28249` - `Move desktop image scaling off draw path`
  - Deferred because it modifies the skipped custom image-loading path and
    conflicts in shelf and continue-watching image code.
  - `0.1.24` already has newer image decode/filter handling, so this older
    desktop prescale path is not added here.

- `b5e9d795` - `Optimize hero parallax and desktop image resampling`
  - Deferred because it conflicts with `0.1.24` `HomeHeroSection` and modifies
    desktop image helper files that were not introduced after earlier image
    commits were skipped.
  - The release tag keeps its current hero/parallax and image handling pending a
    separate comparison.

- `e2bff784` - `Fix desktop image prescale aspect ratio`
  - Deferred because it depends on the skipped desktop prescale/resampling
    helpers and conflicts as modify/delete across common, desktop, Android, and
    iOS image helper files.
  - Keeping upstream avoids recreating the skipped image helper stack.

- `2d081764` - `Improve desktop image downsampling quality`
  - Deferred as part of the same skipped desktop image-resampling stack.
  - It modifies helper files that are absent after keeping `0.1.24` image
    handling, so replaying it would reintroduce that older stack.

- `f945a1d5` - `Use progressive desktop thumbnail resizing`
  - Deferred as part of the skipped desktop image-resampling helper chain.
  - It modifies `DesktopImagePrescale` and `DesktopImageResampling`, which were
    not carried after preserving upstream image handling.

- `7cc07c82` - `Limit progressive resize to desktop thumbnails`
  - Deferred as part of the skipped desktop image-resampling helper chain.
  - It depends on the same helper files that were not carried onto `0.1.24`.

- `d4e3f646` - `Improve desktop hero image rendering`
  - Deferred because it conflicts in shared hero components and collection-card
    desktop rendering while also depending on skipped image wrapper/resampling
    helpers.
  - The newer release hero/image implementation is kept intact.

- `6fa4d06c` - `Use Lanczos desktop image resampling`
  - Deferred because it is a broad image-stack conflict spanning Gradle
    dependencies, common/platform image helper files, shelf rendering, and hero
    call sites.
  - Replaying it would rebuild the skipped image-resampling stack over newer
    `0.1.24` image behavior.

- `8dde1a82` - `Upgrade Metahub images on desktop`
  - Deferred as part of the skipped image-quality stack.
  - It conflicts as modify/delete across platform filter-quality helpers,
    TMDB/Metahub quality utilities, and desktop resampling helpers that were not
    carried onto `0.1.24`.

- `01afa31a` - `Preserve alpha in desktop Lanczos images`
  - Deferred because it only modifies/tests the skipped desktop Lanczos
    resampling helper.
  - Without carrying the helper stack, this commit has no safe target to apply.

- `d946df20` - `Use default painter for transparent logos`
  - Deferred because it depends on the skipped custom `AsyncImage` wrapper and
    conflicts across shelf, details, home hero, and overlay image call sites.
  - Applying it would continue rebuilding the skipped image path over `0.1.24`.

- `44ace62d` - `Dump desktop hero logos for debugging`
  - Deferred because it is diagnostic-only and conflicts with the newer
    `0.1.24` `HomeHeroSection`.
  - Replaying debug dumping code would add non-release behavior while touching
    a component already changed upstream.

- `03c66079` - `Restore mediamp submodule pointer`
  - Deferred because it would move `mediamp` from the `0.1.24` release pointer
    `168a7d62` back to the older `windows-nuvio` branch pointer `8449e27`.
  - The submodule histories diverge at `cbff74c`; `0.1.24` carries a newer
    `windows-nuvio-v2` MPV/build branch, while this commit restores an older
    branch line. Applying it would overwrite release submodule work rather than
    layer a small change on top.
  - Later submodule commits may still be evaluated individually if they can be
    applied or merged without replacing the release branch.

- `f2d6d1ed` - `Split discover from search navigation`
  - Deferred because it is a broad navigation rewrite that conflicts in
    `App.kt`, `SearchScreen`, and `SearchDiscoverContent`.
  - The patch removes the `Library` tab/callback path and replaces native
    profile-tab behavior while `0.1.24` contains newer cloud library and
    profile/native-tab work. Replaying it would overwrite release navigation
    behavior rather than cleanly layering a small Discover change.
  - Later search/navigation commits should still be evaluated individually if
    they apply without removing the release library/profile flow.

- `334883c5` - `Add desktop profile quick switch menu`
  - Deferred because the tablet top-bar conflict is tied to the skipped
    Discover/navigation rewrite from `f2d6d1ed`.
  - `0.1.24` already has the newer `ProfileSwitcherTab` implementation for
    active-profile display, long-press/context-menu quick switching, PIN
    handling, and add-profile flow in the release Library/Profile tab layout.
  - Replaying this commit would add a second tablet profile selector or replace
    the release profile tab behavior, so upstream is kept under the duplicate
    implementation rule.

- `81ab8c75` - `Update mediamp with Windows MPV v2 fixes`
  - Deferred because it would replace the `0.1.24` `mediamp` pointer
    `168a7d62` (`origin/windows-nuvio-v2`) with `81f78c99` from the divergent
    local `windows-nuvio` line.
  - The incoming submodule commit ports selected Windows MPV v2 changes, but
    the release-side submodule already exposes the native render-surface APIs
    needed by the app. A direct submodule merge between `168a7d62` and the
    local `windows-nuvio` line produced broad content/binary conflicts.
  - Keeping the release pointer avoids overwriting newer upstream submodule
    work. Later app-side player commits should still be evaluated individually
    and can use the release submodule APIs where compatible.

- `f7510952` - `Harden desktop mpv rendering teardown` submodule pointer only
  - The compatible app-side teardown hardening was rebased, but the `mediamp`
    gitlink update from `0342ac51` to `571a5828` was not recorded.
  - That gitlink is still on the same divergent local `windows-nuvio` line as
    the earlier deferred submodule commits. Replacing the `0.1.24` release
    pointer `168a7d62` would overwrite the newer release submodule branch and
    reintroduce the broad submodule merge conflicts already observed.

- `c3a7551b` - `Restore safe overlay and sync mpv to display` submodule pointer only
  - The compatible desktop overlay/settings changes were rebased, but the
    `mediamp` gitlink update from `571a5828` to `985f9d17` was not recorded.
  - `985f9d17` remains on the divergent local submodule history. Keeping
    `0.1.24`'s `168a7d62` pointer avoids replacing the release submodule with
    unresolved local branch work while still allowing the app-side overlay
    changes to land.

- `87f41549` - `Optimize desktop render pipeline` image helper stack only
  - The compatible watched-badge and Skiko runtime defaults were rebased, but
    the desktop `AsyncImagePainterWorkaround`, `DesktopImagePrescale`, and
    `DesktopImageResampling` file changes were not restored.
  - Those files belong to the custom desktop image pipeline already deferred
    by earlier image/Lanczos commits. Reintroducing them here would resurrect a
    deleted helper stack over `0.1.24` rather than applying a small isolated
    change.

- `04a250da` - `Consolidate details and source picker`
  - Deferred because it is a broad `App.kt` route/navigation refactor with
    massive conflicts against the current `0.1.24`-based stream route.
  - The incoming version reintroduces the older `Discover` tab model and
    replaces the current route-level stream handling with a consolidated helper
    that does not carry the newer debrid resolution, external-player launch,
    dev-stream, and current tab/profile behavior already present upstream.
  - Applying it would overwrite newer release behavior rather than safely
    layering a source-picker cleanup, so the `0.1.24` implementation is kept.

- `5bf1410c` - `Autoload sources on details page`
  - Deferred because it is built on top of the skipped `04a250da` consolidated
    source-picker route and repeats the same large `App.kt`, details, and
    streams conflicts.
  - Replaying it would require replacing the current stream route with the
    skipped `StreamLaunchContent` architecture, which would risk dropping the
    newer debrid resolution, external-player handling, dev-stream handling, and
    release navigation/profile flow.
  - The current `0.1.24` implementation is kept rather than partially applying
    an autoload UI over incompatible source-picker plumbing.

- `96b5f58b` - `Restyle details page for desktop sources`
  - Deferred because it is a styling/layout follow-up to the skipped inline
    source-picker work from `04a250da` and `5bf1410c`.
  - The patch rewrites large parts of `MetaDetailsScreen` and embedded streams
    layout around source content that was not rebased. Applying it without the
    skipped source-picker route would leave disconnected UI changes and risks
    overwriting the current release details behavior.

- `da226861` - `Reuse detail components for desktop layout`
  - Deferred because it continues the skipped desktop source-panel/detail
    layout chain and conflicts heavily in `MetaDetailsScreen` and detail action
    components.
  - Its component API and settings-string changes are coupled to the skipped
    desktop source layout. Applying only those pieces would create unused or
    misleading configuration while still risking overwrite of the release
    detail screen implementation.

- `5dbfd877` - `Tune desktop detail styling`
  - Deferred because it only tunes the desktop source-panel/detail layout from
    the skipped source-picker chain.
  - The conflicts are concentrated in `MetaDetailsScreen`, embedded streams
    layout, and detail action button styling. Applying it would reintroduce the
    skipped embedded source-panel UI or leave unused styling parameters in the
    release detail/stream screens.

- `3b3f6ce5` - `Polish desktop browse and detail layout`
  - Deferred because it spans the skipped desktop source-panel detail layout and
    the old `Discover` tab path, including a modify/delete conflict for
    `DiscoverScreen.kt`, which the `0.1.24`-based branch no longer uses.
  - The patch also reintroduces the Home shelf `maxItems` truncation that would
    hide scrollable catalog items. Keeping upstream search/tab behavior and the
    current detail layout avoids overwriting newer release architecture.

- `25311fda` - `Improve desktop detail sources and playback controls`
  - Deferred because it combines the skipped desktop detail source-panel work
    with broad player, stream repository, home, and stream-screen changes.
  - The useful cache scoping is already present upstream via
    `StreamLinkCacheRepository.contentKey(type, videoId, parentMetaId, season,
    episode)`, while current player controls and `StreamsRepository` include
    newer lock, parental/video settings, skip-intro, debrid, and stream-loading
    behavior. Applying this commit would overwrite those newer implementations
    with an older source/cache architecture.

- `9f061354` - `Polish desktop detail source layout`
  - Deferred because it is a direct follow-up to the skipped desktop
    source-panel/detail layout and conflicts in the same App, details, and
    embedded streams areas.
  - The horizontal lazy-row gesture API was already rebased in the compatible
    episode-selection commit, so replaying this commit would mostly restore the
    skipped source layout rather than add new independent behavior.

- `fdf30488` - `Polish detail season controls`
  - Deferred because its independent season-control improvements overlap with
    the already rebased `3ed323c3` subset, while the remaining conflicts are in
    the skipped detail source-panel route and embedded stream layout.
  - Replaying it would add more UI churn around the same skipped source path
    rather than preserving a distinct release behavior.

- `0ed35bc2` - `Refine dropdowns and player loading state`
  - Deferred because it depends on the previously deferred dropdown/source-cache
    series and conflicts with the current `0.1.24` player controls, search
    discover UI, and debrid-aware stream repository.
  - The current player stack already has newer lock/video-settings/parental and
    skip-intro behavior, so applying this commit would regress that API surface
    while also reintroducing the skipped dropdown/source-cache path.

- `b8fa5ae7` - `Throttle stream preloading and polish menus`
  - Deferred because it builds directly on the deferred source-cache/preload and
    dropdown menu changes, conflicting in `StreamsRepository`, home shelves, and
    shared UI components.
  - The current release stream repository contains newer debrid-aware loading
    behavior, so this older preload throttling path is not safe to apply
    independently.

- `063d9e8b` - `Experiment with Kache source caching` (partial)
  - Partially rebased by keeping the Kache source-response cache API and routing
    stream-source HTTP fetches through it, with manual refresh bypassing the
    source cache.
  - Deferred the commit's older Home prefetch and repository-level source-cache
    rewrite because those hunks conflicted with the current debrid-aware stream
    repository and the newer `AddonStreamWarmupRepository` path already present
    on `0.1.24`.

- `ef2cedec` - `ui: add desktop image sampling controls`
  - Deferred because it attempts to modify and restore the older cross-platform
    `AsyncImage`/desktop image resampling helper stack that is deleted on the
    `0.1.24` base.
  - The remaining conflicts span detail, hero, player overlay, and stream
    layouts. Applying the commit would reintroduce the deleted image pipeline
    while overwriting current release UI behavior, so the current implementation
    is kept.

- `c8c75193` - `ui: render startup wordmark with exact sampler`
  - Deferred because it depends on the skipped desktop image sampler helpers
    from `ef2cedec`.
  - Its desktop `NuvioWordmarkImage` implementation calls the deleted
    `nuvioScaleTo*`/sampling APIs. Rebasing only the wrapper would drop the
    commit's exact-sampler behavior, while rebasing the full patch would
    resurrect the skipped image pipeline.

- `1af4ebe7` - `Gradient fixes and prefetching`
  - Deferred because it produced massive conflicts across build config, app
    shell, auth, details, home, player overlays, subtitles, settings, streams,
    watch-progress enrichment, and desktop renderer/settings code.
  - The patch also restores the deleted desktop image helper stack
    (`AsyncImagePainterWorkaround`, `DesktopImageResampling`, scaled bitmap
    cache, and gradient background helpers) that earlier image/sampler commits
    were intentionally deferred from the `0.1.24` rebase.
  - Applying it in this pass would risk overwriting newer release UI/player and
    image-rendering behavior with an older broad styling/prefetch chain, so the
    current `0.1.24` implementation is kept for a dedicated follow-up.
